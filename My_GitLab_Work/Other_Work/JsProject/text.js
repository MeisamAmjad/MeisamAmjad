// Just for Test
function onLoad() {
    alert("Function onLoad() just for test");
}

function checkNumber() {
    var theNumber, theMessage;

    //theNumber = documnet.getElementById("somehting").value;

    if (isNaN(theNumber) || theNumber < 1 || thenumber > 10) {
        theMessage = "Number ws expected to be between 1 to 10";
    } else {
        theMessage = "sthg";
    }
    //documnet.getElementById("numberMessage").innerHTML = theMessage;
}